version: '3.8'

services:
  web:
    build: .
    container_name: unicasoluciones_landing
    restart: always
    ports:
      - "8081:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    environment:
      - NGINX_PORT=80
